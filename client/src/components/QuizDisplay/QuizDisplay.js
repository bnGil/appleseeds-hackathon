const QuizDisplay = ({ dataLng }) => {
  // const randomAttraction =
  // Math.floor(Math.random()*items.length)
  const inserQuiz = () => {};
  return <div className="quiz-container">{/*       <div>{insertQuiz()}</div> */}</div>;
};

export default QuizDisplay;
